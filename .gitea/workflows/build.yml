name: CI

on:
    push:
        branches: [main]

jobs:
    quality:
        name: Quality Check
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup Bun
              uses: oven-sh/setup-bun@v1
              with:
                  bun-version: latest

            - name: Install Dependencies
              run: bun install

            - name: Lint
              run: bun run lint

            - name: Build
              run: bun run build

            - name: Test
              run: bun run test

            - name: Test dist out files
              run: bun run test:dist

            - name: Test benchmarks
              run: bun run test:benchmarks

            - name: Run benchmarks
              run: bun run benchmark
